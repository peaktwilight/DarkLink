# Use `panic = "abort"`, `lto = true`
[package]
name = "MicroC2Agent"
version = "0.1.0"
edition = "2025"

[dependencies]
tokio = { version = "1.44.1", features = ["full"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }